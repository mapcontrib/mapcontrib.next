{
  "name": "mapcontrib.next",
  "version": "2.0.0-alpha.3",
  "private": true,
  "devDependencies": {
    "husky": "^0.14.1",
    "lerna": "^2.11.0"
  },
  "scripts": {
    "precommit": "make pre-commit",
    "preversion": "npm run test-ci && npm run build",
    "postversion": "git push && git push --tags"
  },
  "dependencies": {}
}
